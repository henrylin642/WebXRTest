# 2026-02-04 開發摘要

今日主要針對 WebXR 專案進行了大量功能擴展、追蹤穩定化以及 UI/UX 優化。以下是今日修改的重點摘要：

## 1. WebXR 影像追蹤 (Image Tracking) 重構與優化
*   **影像追蹤實現**：正式導入 WebXR Image Tracking API，使用實體海報（ref.jpg）作為追蹤基準。
*   **座標系校正**：
    *   修正了座標軸偏向（將 X 軸旋轉 -90 度以符合 Y-up 垂直海報佈景）。
    *   調整 Z 軸偏移量（Offset），確保虛擬內容與海報上的 LED 實體位置精確對齊。
*   **重力對齊 (Gravity Alignment)**：
    *   實現了 Z-Down 向量對齊邏輯，有效修正因海報張貼角度不平整導致的虛擬物體傾斜問題。
    *   加入了重力調試箭頭 (Gravity Debug Arrow) 方便開發期間驗證方向。
*   **追蹤鎖定機制**：實作了「追蹤成功後鎖定位置」的邏輯，避免因追蹤中斷導致虛擬內容大幅晃動。

## 2. 功能增強
*   **快照功能 (Snapshot) 的進化與實現**：
    *   **最初階段**：實現基本的 UI 隱藏模式，供使用者手動截圖。
    *   **最終突破**：成功利用 `camera-access` 實驗性 API 與 `XRWebGLBinding` 技術，實現了**「自動合成快照」**功能。程式會在背景提取相機原始像素，並透過離屏渲染（RenderTarget）將 3D 場景精確疊加在實景之上。
    *   **分享優化**：導入 Web Share API，完成截圖後自動彈出系統分享選單，大幅提升使用者儲存圖片的便利性。
*   **SLAM 品質監控器**：在 UI 上新增了 FPS 指標與追蹤狀態監測（Tracking State），便於掌握裝置效能與 AR 穩定性。
*   **相機權限擴展**：在 Session 初始化時請求 `camera-access` 實驗性功能，為未來更精細的取景與影像存取做準備。

## 3. UI/UX 與除錯工具
*   **AR 引導流程 (Onboarding)**：新增了分步式引導 UI，指導使用者在啟動 Session 前對準海報。
*   **調試輔助**：
    *   新增 `AxesHelper` 標註座標原點。
    *   新增即時 6DoF 相機位置數據 (XYZ/Rotation) 疊加顯示。
*   **UI 可見性**：修正了 WebXR 模式下部分 HTML 元素無法正確顯示或點擊的問題。

## 4. 系統修復與部署
*   **Bug Pixes**：
    *   修正了 `isImageFound` 與 `currentWidth` 的全域變數引用錯誤。
    *   修復了導致 AR 崩潰的 `onSelect` 事件處理缺失問題。
    *   更正了渲染循環 (Render Loop) 的語法細節，提高渲染效率。
*   **部署配置**：優化 `vite.config.mjs`，確保在 Vercel 環境下載入資源與構建過程正確無誤。

## 5. 高階相簿與快照體驗優化 (Advanced Gallery & Snapshot UX)
*   **靜默快照流水線**：
    *   **色彩空間校正**：強制使用 `SRGBColorSpace`，修復了截圖中 3D 物件光影灰暗、缺乏質感的問題。
    *   **渲染權限切換**：在截圖瞬間暫時關閉 `renderer.xr.enabled`，以獲得標準的高品質物理渲染效果。
    *   **取消硬性彈窗**：移除拍照後強制的分享跳轉，改為静默存入快照快取，避免打斷 AR 連續拍攝體驗。
*   **智慧縮略圖系統**：
    *   實作了置於快門上方的**磨砂玻璃風格橫條**。
    *   支援「最近 3 張預覽 + 超量計數 (`+N`)」邏輯。
*   **AR 內全功能預覽**：
    *   **大圖檢視**：點擊縮略圖可直接在 AR 環境下開啟 1:1 大圖。
    *   **多樣化動作**：預覽介面整合「💾 儲存 (下載)」與「🔗 系統分享」功能。
    *   **避障 UI 設計**：將關閉按鈕移至底部「✖️ 返回」位置，解決了手機端下載通知會遮擋螢幕上方「X」按鈕的問題。
*   **完整相簿網格 (Full Gallery)**：
    *   點擊 `+N` 可展開全屏網格檢視，按拍攝順序緊湊排列。
    *   支援垂直捲動與歷史照片的二次預覽/儲存。
*   **Session 管理優化**：
    *   **一鍵退出**：新增「🏠 返回首頁」按鈕，可主動結束 WebXR Session。
    *   **智慧顯隱控制**：所有 AR 專屬 UI 工具（快門、設定、狀態等）僅在 Session 激活時顯示，啟動前與結束後自動隱藏，維持首頁整潔。

---
*記錄者：Antigravity AI*
