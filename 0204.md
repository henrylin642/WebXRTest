# 2026-02-04 開發摘要

今日主要針對 WebXR 專案進行了大量功能擴展、追蹤穩定化以及 UI/UX 優化。以下是今日修改的重點摘要：

## 1. WebXR 影像追蹤 (Image Tracking) 重構與優化
*   **影像追蹤實現**：正式導入 WebXR Image Tracking API，使用實體海報（ref.jpg）作為追蹤基準。
*   **座標系校正**：
    *   修正了座標軸偏向（將 X 軸旋轉 -90 度以符合 Y-up 垂直海報佈景）。
    *   調整 Z 軸偏移量（Offset），確保虛擬內容與海報上的 LED 實體位置精確對齊。
*   **重力對齊 (Gravity Alignment)**：
    *   實現了 Z-Down 向量對齊邏輯，有效修正因海報張貼角度不平整導致的虛擬物體傾斜問題。
    *   加入了重力調試箭頭 (Gravity Debug Arrow) 方便開發期間驗證方向。
*   **追蹤鎖定機制**：實作了「追蹤成功後鎖定位置」的邏輯，避免因追蹤中斷導致虛擬內容大幅晃動。

## 2. 功能增強
*   **快照功能 (Snapshot)**：
    *   開發了螢幕截圖快照功能。
    *   經歷了從「複雜相冊模式 (Gallery)」到「簡易 UI 隱藏模式」的優化改版，解決了底層 Canvas 渲染及 UI 重疊的相容性問題。
*   **SLAM 品質監控器**：在 UI 上新增了 FPS 指標與追蹤狀態監測（Tracking State），便於掌握裝置效能與 AR 穩定性。
*   **相機權限擴展**：在 Session 初始化時請求 `camera-access` 實驗性功能，為未來更精細的取景與影像存取做準備。

## 3. UI/UX 與除錯工具
*   **AR 引導流程 (Onboarding)**：新增了分步式引導 UI，指導使用者在啟動 Session 前對準海報。
*   **調試輔助**：
    *   新增 `AxesHelper` 標註座標原點。
    *   新增即時 6DoF 相機位置數據 (XYZ/Rotation) 疊加顯示。
*   **UI 可見性**：修正了 WebXR 模式下部分 HTML 元素無法正確顯示或點擊的問題。

## 4. 系統修復與部署
*   **Bug Pixes**：
    *   修正了 `isImageFound` 與 `currentWidth` 的全域變數引用錯誤。
    *   修復了導致 AR 崩潰的 `onSelect` 事件處理缺失問題。
    *   更正了渲染循環 (Render Loop) 的語法細節，提高渲染效率。
*   **部署配置**：優化 `vite.config.mjs`，確保在 Vercel 環境下載入資源與構建過程正確無誤。

---
*記錄者：Antigravity AI*
